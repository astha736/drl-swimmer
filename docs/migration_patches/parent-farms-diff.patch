diff --git a/farms/farms_amphibious/farms_amphibious/data/network.py b/farms/farms_amphibious/farms_amphibious/data/network.py
index 2e2df4cf..4e7d215e 100644
--- a/farms/farms_amphibious/farms_amphibious/data/network.py
+++ b/farms/farms_amphibious/farms_amphibious/data/network.py
@@ -19,6 +19,7 @@ from farms_core.array.types import (
     NDARRAY_V2_D,
 )
 
+from ..model.options import AmphibiousOptions
 from .data_cy import (
     ConnectionType,
     NetworkParametersCy,
@@ -39,47 +40,27 @@ from .data_cy import (
 NPDTYPE = np.float64
 NPUITYPE = np.uintc
 
-# require 10 values for 10 oscillators
-gradient = [
-    "#182A34",
-    "#2a4858",
-    "#215d6e",
-    "#08737f",
-    "#00898a",
-    "#089f8f",
-    "#39b48e",
-    "#64c987",
-    "#92dc7e",
-    "#c4ec74",
-    "#fafa6e",
-    "#f6e86d",
-]
-
-# TODO put this at a difference location and use self.n_oscillators
-right_oscillator_indexes = [i * 2 - 1 for i in range(1, 11)]
-left_oscillator_indexes = [i * 2 for i in range(0, 10)]
-
 
 CONNECTIONTYPENAMES = [
-    "OSC2OSC",
-    "DRIVE2OSC",
-    "POS2FREQ",
-    "VEL2FREQ",
-    "TOR2FREQ",
-    "POS2AMP",
-    "VEL2AMP",
-    "TOR2AMP",
-    "STRETCH2FREQTEGOTAE",
-    "STRETCH2AMPTEGOTAE",
-    "STRETCH2FREQ",
-    "STRETCH2AMP",
-    "REACTION2FREQ",
-    "REACTION2AMP",
-    "REACTION2FREQTEGOTAE",
-    "FRICTION2FREQ",
-    "FRICTION2AMP",
-    "LATERAL2FREQ",
-    "LATERAL2AMP",
+    'OSC2OSC',
+    'DRIVE2OSC',
+    'POS2FREQ',
+    'VEL2FREQ',
+    'TOR2FREQ',
+    'POS2AMP',
+    'VEL2AMP',
+    'TOR2AMP',
+    'STRETCH2FREQTEGOTAE',
+    'STRETCH2AMPTEGOTAE',
+    'STRETCH2FREQ',
+    'STRETCH2AMP',
+    'REACTION2FREQ',
+    'REACTION2AMP',
+    'REACTION2FREQTEGOTAE',
+    'FRICTION2FREQ',
+    'FRICTION2AMP',
+    'LATERAL2FREQ',
+    'LATERAL2AMP',
 ]
 assert len(ConnectionType) == len(CONNECTIONTYPENAMES)
 CONNECTIONTYPE2NAME = dict(zip(ConnectionType, CONNECTIONTYPENAMES))
@@ -87,26 +68,26 @@ NAME2CONNECTIONTYPE = dict(zip(CONNECTIONTYPENAMES, ConnectionType))
 
 
 def connections_from_connectivity(
-    connectivity: List[Dict],
-    map1: Dict = None,
-    map2: Dict = None,
+        connectivity: List[Dict],
+        map1: Dict = None,
+        map2: Dict = None,
 ) -> List[Tuple[int, int, int]]:
     """Connections from connectivity"""
     if map1 or map2:
         for connection in connectivity:
             if map1:
-                assert (
-                    connection["in"] in map1
-                ), f'Connection {connection["in"]} not in map {map1}'
+                assert connection['in'] in map1, (
+                    f'Connection {connection["in"]} not in map {map1}'
+                )
             if map2:
-                assert (
-                    connection["out"] in map2
-                ), f'Connection {connection["out"]} not in map {map2}'
+                assert connection['out'] in map2, (
+                    f'Connection {connection["out"]} not in map {map2}'
+                )
     return [
         [
-            map1[connection["in"]] if map1 else connection["in"],
-            map2[connection["out"]] if map2 else connection["out"],
-            NAME2CONNECTIONTYPE[connection["type"]],
+            map1[connection['in']] if map1 else connection['in'],
+            map2[connection['out']] if map2 else connection['out'],
+            NAME2CONNECTIONTYPE[connection['type']]
         ]
         for connection in connectivity
     ]
@@ -117,10 +98,10 @@ class OscillatorNetworkState(OscillatorNetworkStateCy):
 
     @classmethod
     def from_initial_state(
-        cls,
-        initial_state: NDARRAY_V1,
-        n_iterations: int,
-        n_oscillators: int,
+            cls,
+            initial_state: NDARRAY_V1,
+            n_iterations: int,
+            n_oscillators: int,
     ):
         """From initial state"""
         state_size = len(initial_state)
@@ -135,115 +116,49 @@ class OscillatorNetworkState(OscillatorNetworkStateCy):
     def plot(self, times: NDARRAY_V1) -> Dict:
         """Plot"""
         return {
-            "phases": self.plot_phases(times),
-            "amplitudes": self.plot_amplitudes(times),
-            "neural_activity_normalised": (self.plot_neural_activity_normalised(times)),
+            'phases': self.plot_phases(times),
+            'amplitudes': self.plot_amplitudes(times),
+            'neural_activity_normalised': (
+                self.plot_neural_activity_normalised(times)
+            ),
         }
 
-    def get_phase_differences(
-        self,
-    ):
-        """Get phase differences; for one side is enough, other side should be equivalent"""
-        right = np.transpose(self.phases_all())[right_oscillator_indexes]
-        return np.round(np.diff(right, axis=0), decimals=2)
-
-    def get_phase_differences_sum(
-        self,
-    ):
-        return np.sum(self.get_phase_differences(), axis=0)
-
-    def plot_phase_differences(
-        self,
-        times: NDARRAY_V1,
-        sum: bool = True,
-    ) -> Figure:
-        fig = plt.figure(f"Oscillator phase lags")
-        phase_differences = self.get_phase_differences()
-        for i, data in enumerate(np.array(phase_differences)):
-            plt.plot(
-                times,
-                data[: len(times)],
-                label=f"Joint {i} - {i+1}",  # , color=gradient[i]
-            )
-        if sum:
-            plt.plot(
-                times,
-                self.get_phase_differences_sum()[: len(times)],
-                color="red",
-                label="Sum",
-            )
-        plt.legend(
-            bbox_to_anchor=(1.05, 1),
-            borderaxespad=0,
-        )
-        plt.xlabel("Times [s]")
-        plt.ylabel("Phase differences [rad]")
-        plt.grid(True)
-        return fig
-
     def plot_phases(
-        self,
-        times: NDARRAY_V1,
-        side: str = "right",  # right or left
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot phases"""
-        fig = plt.figure(f"Network state phases {side}")
-        osc_index = (
-            right_oscillator_indexes if side == "right" else left_oscillator_indexes
-        )
-        _i = 0
-        # TODO this for loop can avoided; rewrite code and use numpy indexing with lists
-        for i, data in enumerate(np.transpose(self.phases_all())):
-            if i in osc_index:
-                _i += 1
-                plt.plot(
-                    times,
-                    data[: len(times)],
-                    label=f"Osci {i}",  # color=gradient[_i]
-                )
-        plt.legend(
-            bbox_to_anchor=(1.05, 1),
-            borderaxespad=0,
-        )
-        plt.xlabel("Times [s]")
-        plt.ylabel("Phases [rad]")
+        fig = plt.figure('Network state phases')
+        for data in np.transpose(self.phases_all()):
+            plt.plot(times, data[:len(times)])
+        plt.xlabel('Times [s]')
+        plt.ylabel('Phases [rad]')
         plt.grid(True)
         return fig
 
-    def plots(
-        self,
-        times: NDARRAY_V1,
-    ) -> Dict:
-        """Plots"""
-        return {
-            "plots_phases (right oscillators)": self.plot_phases(times),
-            "phase_differences (right oscillators)": self.plot_phase_differences(times),
-            "neural activity normalised": self.plot_neural_activity_normalised(times),
-        }
-
     def plot_amplitudes(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot amplitudes"""
-        fig = plt.figure("Network state amplitudes")
+        fig = plt.figure('Network state amplitudes')
         for data in np.transpose(self.amplitudes_all()):
-            plt.plot(times, data[: len(times)])
-        plt.xlabel("Times [s]")
-        plt.ylabel("Amplitudes")
+            plt.plot(times, data[:len(times)])
+        plt.xlabel('Times [s]')
+        plt.ylabel('Amplitudes')
         plt.grid(True)
         return fig
 
     def plot_neural_activity_normalised(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot amplitudes"""
-        fig = plt.figure("Neural activities (normalised)")
+        fig = plt.figure('Neural activities (normalised)')
         for data_i, data in enumerate(np.transpose(self.phases_all())):
-            plt.plot(times, 2 * data_i + 0.5 * (1 + np.cos(data[: len(times)])))
-        plt.xlabel("Times [s]")
-        plt.ylabel("Neural activity")
+            plt.plot(times, 2*data_i + 0.5*(1 + np.cos(data[:len(times)])))
+        plt.xlabel('Times [s]')
+        plt.ylabel('Neural activity')
         plt.grid(True)
         return fig
 
@@ -251,13 +166,29 @@ class OscillatorNetworkState(OscillatorNetworkStateCy):
 class DriveArray(DriveArrayCy):
     """Drive array"""
 
+    def __init__(
+            self,
+            array: NDARRAY_V2_D,
+            left_indices: NDARRAY_V1_I,
+            right_indices: NDARRAY_V1_I,
+            contacts_indices: List[List[int]] = None,
+    ):
+        super().__init__(
+            array=array,
+            left_indices=left_indices,
+            right_indices=right_indices,
+        )
+        self.contacts_indices = contacts_indices
+
     @classmethod
-    def from_initial_drive(
-        cls,
-        initial_drives: List[float],
-        n_iterations: int,
+    def from_animat_options(
+            cls,
+            animat_options: AmphibiousOptions,
+            n_iterations: int,
     ):
         """From initial drive"""
+        control = animat_options.control
+        initial_drives = control.network.drives_init()
         drive_size = len(initial_drives)
         drive_array = np.full(
             shape=[n_iterations, drive_size],
@@ -265,18 +196,58 @@ class DriveArray(DriveArrayCy):
             dtype=NPDTYPE,
         )
         drive_array[0, :] = initial_drives
-        return cls(drive_array)
+        return cls(
+            array=drive_array,
+            left_indices=control.network.drives_left_indices(),
+            right_indices=control.network.drives_right_indices(),
+            contacts_indices=control.drives_contacts_indices(),
+        )
+
+    @classmethod
+    def from_dict(cls, dictionary: Dict):
+        """Load data from dictionary"""
+        contacts_indices = [
+            [index for index in indices if not index < 0]
+            for indices in dictionary['contacts_indices']
+        ]
+        return cls(
+            array=dictionary['array'],
+            left_indices=dictionary['left_indices'],
+            right_indices=dictionary['right_indices'],
+            contacts_indices=contacts_indices,
+        )
+
+    def to_dict(self, iteration: int = None) -> Dict:
+        """Convert data to dictionary"""
+        assert iteration is None or isinstance(iteration, int)
+        contacts_indices = self.contacts_indices
+        if contacts_indices:
+            maxlen = max(len(indices) for indices in contacts_indices)
+            contacts_indices = np.full(
+                shape=[len(contacts_indices), maxlen],
+                fill_value=-1,
+            )
+            positives =  np.arange(self.array.shape[0])
+            for indices_i, indices in enumerate(self.contacts_indices):
+                for index_i, index in enumerate(indices):
+                    contacts_indices[indices_i, index_i] = positives[index]
+        return {
+            'array': self.array,
+            'left_indices': to_array(self.left_indices),
+            'right_indices': to_array(self.right_indices),
+            'contacts_indices': contacts_indices,
+        }
 
     def plot(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot phases"""
-        fig = plt.figure("Drives")
+        fig = plt.figure('Drives')
         for i, data in enumerate(np.transpose(np.array(self.array))):
-            plt.plot(times, data[: len(times)], label=i)
-        plt.xlabel("Times [s]")
-        plt.ylabel("Drive value")
+            plt.plot(times, data[:len(times)], label=i)
+        plt.xlabel('Times [s]')
+        plt.ylabel('Drive value')
         plt.grid(True)
         plt.legend()
         return fig
@@ -287,12 +258,12 @@ class DriveDependentArray(DriveDependentArrayCy):
 
     @classmethod
     def from_vectors(
-        cls,
-        gain: float,
-        bias: float,
-        low: float,
-        high: float,
-        saturation: float,
+            cls,
+            gain: float,
+            bias: float,
+            low: float,
+            high: float,
+            saturation: float,
     ):
         """From each parameter"""
         return cls(np.array([gain, bias, low, high, saturation]))
@@ -302,14 +273,14 @@ class Oscillators(OscillatorsCy):
     """Oscillator array"""
 
     def __init__(
-        self,
-        names: List[str],
-        drive2osc_map: NDARRAY_V1_I,
-        intrinsic_frequencies: NDARRAY_V2_D,
-        nominal_amplitudes: NDARRAY_V2_D,
-        rates: NDARRAY_V1_D,
-        modular_phases: NDARRAY_V1_D,
-        modular_amplitudes: NDARRAY_V1_D,
+            self,
+            names: List[str],
+            drive2osc_map: NDARRAY_V1_I,
+            intrinsic_frequencies: NDARRAY_V2_D,
+            nominal_amplitudes: NDARRAY_V2_D,
+            rates: NDARRAY_V1_D,
+            modular_phases: NDARRAY_V1_D,
+            modular_amplitudes: NDARRAY_V1_D,
     ):
         super().__init__(
             n_oscillators=len(names),
@@ -326,19 +297,16 @@ class Oscillators(OscillatorsCy):
     def from_options(cls, network):
         """Default"""
         freqs, amplitudes = [
-            np.array(
+            np.array([
                 [
-                    [
-                        freq["gain"],
-                        freq["bias"],
-                        freq["low"],
-                        freq["high"],
-                        freq["saturation"],
-                    ]
-                    for freq in option
-                ],
-                dtype=NPDTYPE,
-            )
+                    freq['gain'],
+                    freq['bias'],
+                    freq['low'],
+                    freq['high'],
+                    freq['saturation'],
+                ]
+                for freq in option
+            ], dtype=NPDTYPE)
             for option in [network.osc_frequencies(), network.osc_amplitudes()]
         ]
         return cls(
@@ -355,26 +323,26 @@ class Oscillators(OscillatorsCy):
     def from_dict(cls, dictionary: Dict):
         """Load data from dictionary"""
         return cls(
-            names=dictionary["names"],
-            drive2osc_map=dictionary["drive2osc_map"],
-            intrinsic_frequencies=dictionary["intrinsic_frequencies"],
-            nominal_amplitudes=dictionary["nominal_amplitudes"],
-            rates=dictionary["rates"],
-            modular_phases=dictionary["modular_phases"],
-            modular_amplitudes=dictionary["modular_amplitudes"],
+            names=dictionary['names'],
+            drive2osc_map=dictionary['drive2osc_map'],
+            intrinsic_frequencies=dictionary['intrinsic_frequencies'],
+            nominal_amplitudes=dictionary['nominal_amplitudes'],
+            rates=dictionary['rates'],
+            modular_phases=dictionary['modular_phases'],
+            modular_amplitudes=dictionary['modular_amplitudes'],
         )
 
     def to_dict(self, iteration: int = None) -> Dict:
         """Convert data to dictionary"""
         assert iteration is None or isinstance(iteration, int)
         return {
-            "names": self.names,
-            "drive2osc_map": to_array(self.drive2osc_map.array),
-            "intrinsic_frequencies": to_array(self.intrinsic_frequencies.array),
-            "nominal_amplitudes": to_array(self.nominal_amplitudes.array),
-            "rates": to_array(self.rates.array),
-            "modular_phases": to_array(self.modular_phases.array),
-            "modular_amplitudes": to_array(self.modular_amplitudes.array),
+            'names': self.names,
+            'drive2osc_map': to_array(self.drive2osc_map.array),
+            'intrinsic_frequencies': to_array(self.intrinsic_frequencies.array),
+            'nominal_amplitudes': to_array(self.nominal_amplitudes.array),
+            'rates': to_array(self.rates.array),
+            'modular_phases': to_array(self.modular_phases.array),
+            'modular_amplitudes': to_array(self.modular_amplitudes.array),
         }
 
 
@@ -385,26 +353,32 @@ class OscillatorConnectivity(OscillatorsConnectivityCy):
     def from_dict(cls, dictionary: Dict):
         """Load data from dictionary"""
         return cls(
-            connections=dictionary["connections"],
-            weights=dictionary["weights"],
-            desired_phases=dictionary["desired_phases"],
+            connections=dictionary['connections'],
+            weights=dictionary['weights'],
+            desired_phases=dictionary['desired_phases'],
         )
 
     def to_dict(self, iteration: int = None) -> Dict:
         """Convert data to dictionary"""
         assert iteration is None or isinstance(iteration, int)
         return {
-            "connections": to_array(self.connections.array),
-            "weights": to_array(self.weights.array),
-            "desired_phases": to_array(self.desired_phases.array),
+            'connections': to_array(self.connections.array),
+            'weights': to_array(self.weights.array),
+            'desired_phases': to_array(self.desired_phases.array),
         }
 
     @classmethod
     def from_connectivity(cls, connectivity: List[Dict], **kwargs):
         """From connectivity"""
         connections = connections_from_connectivity(connectivity, **kwargs)
-        weights = [connection["weight"] for connection in connectivity]
-        phase_bias = [connection["phase_bias"] for connection in connectivity]
+        weights = [
+            connection['weight']
+            for connection in connectivity
+        ]
+        phase_bias = [
+            connection['phase_bias']
+            for connection in connectivity
+        ]
         return cls(
             connections=np.array(connections, dtype=NPUITYPE),
             weights=np.array(weights, dtype=NPDTYPE),
@@ -419,7 +393,10 @@ class JointsConnectivity(JointsConnectivityCy):
     def from_connectivity(cls, connectivity: List[Dict], **kwargs):
         """From connectivity"""
         connections = connections_from_connectivity(connectivity, **kwargs)
-        weights = [connection["weight"] for connection in connectivity]
+        weights = [
+            connection['weight']
+            for connection in connectivity
+        ]
         return cls(
             connections=np.array(connections, dtype=NPUITYPE),
             weights=np.array(weights, dtype=NPDTYPE),
@@ -429,15 +406,15 @@ class JointsConnectivity(JointsConnectivityCy):
     def from_dict(cls, dictionary: Dict):
         """Load data from dictionary"""
         return cls(
-            connections=dictionary["connections"],
-            weights=dictionary["weights"],
+            connections=dictionary['connections'],
+            weights=dictionary['weights'],
         )
 
     def to_dict(self, _iteration: int = None) -> Dict:
         """Convert data to dictionary"""
         return {
-            "connections": to_array(self.connections.array),
-            "weights": to_array(self.weights.array),
+            'connections': to_array(self.connections.array),
+            'weights': to_array(self.weights.array),
         }
 
 
@@ -449,9 +426,11 @@ class ContactsConnectivity(ContactsConnectivityCy):
         """From connectivity"""
         connections = connections_from_connectivity(connectivity, **kwargs)
         assert all(
-            isinstance(val, int) for conn in connections for val in conn
-        ), f"All connections must be integers:\n{connections}"
-        weights = [connection["weight"] for connection in connectivity]
+            isinstance(val, int)
+            for conn in connections
+            for val in conn
+        ), f'All connections must be integers:\n{connections}'
+        weights = [connection['weight'] for connection in connectivity]
         return cls(
             connections=np.array(connections, dtype=NPUITYPE),
             weights=np.array(weights, dtype=NPDTYPE),
@@ -461,15 +440,15 @@ class ContactsConnectivity(ContactsConnectivityCy):
     def from_dict(cls, dictionary: Dict):
         """Load data from dictionary"""
         return cls(
-            connections=dictionary["connections"],
-            weights=dictionary["weights"],
+            connections=dictionary['connections'],
+            weights=dictionary['weights'],
         )
 
     def to_dict(self, _iteration: int = None) -> Dict:
         """Convert data to dictionary"""
         return {
-            "connections": to_array(self.connections.array),
-            "weights": to_array(self.weights.array),
+            'connections': to_array(self.connections.array),
+            'weights': to_array(self.weights.array),
         }
 
 
@@ -480,7 +459,10 @@ class XfrcConnectivity(XfrcConnectivityCy):
     def from_connectivity(cls, connectivity: List[Dict], **kwargs):
         """From connectivity"""
         connections = connections_from_connectivity(connectivity, **kwargs)
-        weights = [connection["weight"] for connection in connectivity]
+        weights = [
+            connection['weight']
+            for connection in connectivity
+        ]
         return cls(
             connections=np.array(connections, dtype=NPUITYPE),
             weights=np.array(weights, dtype=NPDTYPE),
@@ -490,16 +472,16 @@ class XfrcConnectivity(XfrcConnectivityCy):
     def from_dict(cls, dictionary: Dict):
         """Load data from dictionary"""
         return cls(
-            connections=dictionary["connections"],
-            weights=dictionary["weights"],
+            connections=dictionary['connections'],
+            weights=dictionary['weights'],
         )
 
     def to_dict(self, iteration: int = None) -> Dict:
         """Convert data to dictionary"""
         assert iteration is None or isinstance(iteration, int)
         return {
-            "connections": to_array(self.connections.array),
-            "weights": to_array(self.weights.array),
+            'connections': to_array(self.connections.array),
+            'weights': to_array(self.weights.array),
         }
 
 
@@ -509,31 +491,35 @@ class NetworkParameters(NetworkParametersCy):
     @classmethod
     def from_dict(cls, dictionary: Dict):
         """Load data from dictionary"""
-        return (
-            cls(
-                drives=DriveArray(dictionary["drives"]),
-                oscillators=Oscillators.from_dict(dictionary["oscillators"]),
-                osc2osc_map=OscillatorConnectivity.from_dict(dictionary["osc2osc_map"]),
-                joints2osc_map=JointsConnectivity.from_dict(
-                    dictionary["joints2osc_map"]
-                ),
-                contacts2osc_map=ContactsConnectivity.from_dict(
-                    dictionary["contacts2osc_map"]
-                ),
-                xfrc2osc_map=XfrcConnectivity.from_dict(dictionary["xfrc2osc_map"]),
-            )
-            if dictionary
-            else None
-        )
+        return cls(
+            drives=DriveArray.from_dict(
+                dictionary['drives']
+            ),
+            oscillators=Oscillators.from_dict(
+                dictionary['oscillators']
+            ),
+            osc2osc_map=OscillatorConnectivity.from_dict(
+                dictionary['osc2osc_map']
+            ),
+            joints2osc_map=JointsConnectivity.from_dict(
+                dictionary['joints2osc_map']
+            ),
+            contacts2osc_map=ContactsConnectivity.from_dict(
+                dictionary['contacts2osc_map']
+            ),
+            xfrc2osc_map=XfrcConnectivity.from_dict(
+                dictionary['xfrc2osc_map']
+            ),
+        ) if dictionary else None
 
     def to_dict(self, iteration: int = None) -> Dict:
         """Convert data to dictionary"""
         assert iteration is None or isinstance(iteration, int)
         return {
-            "drives": to_array(self.drives.array),
-            "oscillators": self.oscillators.to_dict(),
-            "osc2osc_map": self.osc2osc_map.to_dict(),
-            "joints2osc_map": self.joints2osc_map.to_dict(),
-            "contacts2osc_map": self.contacts2osc_map.to_dict(),
-            "xfrc2osc_map": self.xfrc2osc_map.to_dict(),
+            'drives': self.drives.to_dict(),
+            'oscillators': self.oscillators.to_dict(),
+            'osc2osc_map': self.osc2osc_map.to_dict(),
+            'joints2osc_map': self.joints2osc_map.to_dict(),
+            'contacts2osc_map': self.contacts2osc_map.to_dict(),
+            'xfrc2osc_map': self.xfrc2osc_map.to_dict(),
         }
diff --git a/farms/farms_core/farms_core/sensors/data.py b/farms/farms_core/farms_core/sensors/data.py
index b7ad0d52..75cea78c 100644
--- a/farms/farms_core/farms_core/sensors/data.py
+++ b/farms/farms_core/farms_core/sensors/data.py
@@ -2,9 +2,6 @@
 
 from typing import List, Dict
 import numpy as np
-import matplotlib
-
-matplotlib.use("pdf")
 import matplotlib.pyplot as plt
 from matplotlib.figure import Figure
 from scipy.stats import circmean
@@ -32,10 +29,9 @@ from .data_cy import (
     JointSensorArrayCy,
     ContactsArrayCy,
     XfrcArrayCy,
+    MusclesArrayCy
 )
 
-import conf
-
 # pylint: disable=no-member,unsubscriptable-object
 # pylint: disable=unsupported-assignment-operation
 
@@ -43,108 +39,37 @@ import conf
 NPDTYPE = np.float64
 NPUITYPE = np.uintc
 
-PLOT_ARENA_BORDER = 0.25
-
-# https://colordesigner.io/gradient-generator
-gradient_1 = [
-    "#2a4858",
-    "#215d6e",
-    "#08737f",
-    "#00898a",
-    "#089f8f",
-    "#39b48e",
-    "#64c987",
-    "#92dc7e",
-    "#c4ec74",
-    "#fafa6e",
-    "#ff0000",  # red; plots sum of all sensors
-]
-
-gradient_2 = [
-    "#072140",
-    "#0A2D57",
-    "#0E396E",
-    "#114584",
-    "#14519A",
-    "#165DB1",
-    "#5E94D4",
-    "#9ABCE4",
-    "#C2D7EF",
-    "#FED702",
-    "#CBAB01",
-    "#FEDE34",
-    "#FEE667",
-    # "#F7811E",
-    # "#D99208",
-    # "#F9BF4E",
-    # "#FAD080",
-    # "#B55CA5",
-    # "#9B468D",
-    # "#C680BB",
-    # "#D6A4CE",
-    # "#E6C7E1",
-    # "#8F81EA",
-    # "#6955E2",
-    # "#B6ACF1",
-    # "#C9C2F5",
-    "#9FBA36",
-    "#7D922A",
-    "#B6CE55",
-    "#C7D97D",
-    # "#333A41",
-    # "#475058",
-    # "#6A757E",
-]
-# array with position of pegs in arena
-simulation_arena_geom = {
-    "pegs": [
-        [x, y]
-        for x in np.linspace(start=-1.0, stop=3, num=20)
-        for y in np.linspace(start=-2.0, stop=2.0, num=20)
-    ],
-    "radius": 0.02,  # 6
-}
-
-real_arena_geom = {
-    "pegs": [
-        [x, y]
-        for x in np.arange(-1.0, 3.0, 0.31)
-        for y in np.arange(-1.705, 1.705, 0.31)
-    ],
-    "radius": 0.055,  # not used
-}
-
 
 class SensorData(DoubleArray3D):
     """SensorData"""
 
     def __init__(
-        self,
-        array: NDARRAY_V3_D,
-        names: List[str],
+            self,
+            array: NDARRAY_V3_D,
+            names: List[str],
     ):
         super().__init__(array)
         self.names = names
 
     @classmethod
     def from_dict(
-        cls,
-        dictionary: Dict,
+            cls,
+            dictionary: Dict,
     ):
         """Load data from dictionary"""
         return cls(
-            array=dictionary["array"],
-            names=dictionary["names"],
+            array=dictionary['array'],
+            names=dictionary['names'],
         )
 
     def to_dict(
-        self,
-        iteration: int = None,
+            self,
+            iteration: int = None,
     ) -> Dict:
         """Convert data to dictionary"""
         return {
-            "array": to_array(self.array, iteration),
-            "names": self.names,
+            'array': to_array(self.array, iteration),
+            'names': self.names,
         }
 
 
@@ -153,12 +78,13 @@ class SensorsData(SensorsDataCy):
 
     @classmethod
     def from_names(
-        cls,
-        n_iterations: int,
-        links_names: List[str],
-        joints_names: List[str],
-        contacts_names: List[str],
-        xfrc_names: List[str],
+            cls,
+            n_iterations: int,
+            links_names: List[str],
+            joints_names: List[str],
+            contacts_names: List[str],
+            xfrc_names: List[str],
+            muscles_names: List[str],
     ):
         """From options"""
         return SensorsData(
@@ -178,13 +104,17 @@ class SensorsData(SensorsDataCy):
                 names=xfrc_names,
                 n_iterations=n_iterations,
             ),
+            muscles=MusclesArray.from_names(
+                names=muscles_names,
+                n_iterations=n_iterations,
+            ),
         )
 
     @classmethod
     def from_options(
-        cls,
-        animat_options: AnimatOptions,
-        simulation_options: SimulationOptions,
+            cls,
+            animat_options: AnimatOptions,
+            simulation_options: SimulationOptions,
     ):
         """From options"""
         return cls.from_names(
@@ -193,40 +123,43 @@ class SensorsData(SensorsDataCy):
             joints_names=animat_options.control.sensors.joints,
             contacts_names=animat_options.control.sensors.contacts,
             xfrc_names=animat_options.control.sensors.xfrc,
+            muscles_names=animat_options.control.sensors.muscles,
         )
 
     @classmethod
     def from_dict(
-        cls,
-        dictionary: Dict,
+            cls,
+            dictionary: Dict,
     ):
         """Load data from dictionary"""
         return cls(
-            links=LinkSensorArray.from_dict(dictionary["links"]),
-            joints=JointSensorArray.from_dict(dictionary["joints"]),
-            contacts=ContactsArray.from_dict(dictionary["contacts"]),
-            xfrc=XfrcArray.from_dict(dictionary["xfrc"]),
+            links=LinkSensorArray.from_dict(dictionary['links']),
+            joints=JointSensorArray.from_dict(dictionary['joints']),
+            contacts=ContactsArray.from_dict(dictionary['contacts']),
+            xfrc=XfrcArray.from_dict(dictionary['xfrc']),
+            muscles=MusclesArray.from_dict(dictionary['muscles']),
         )
 
     def to_dict(
-        self,
-        iteration: int = None,
+            self,
+            iteration: int = None,
     ) -> Dict:
         """Convert data to dictionary"""
         return {
             name: data.to_dict(iteration)
             for name, data in [
-                ["links", self.links],
-                ["joints", self.joints],
-                ["contacts", self.contacts],
-                ["xfrc", self.xfrc],
+                ['links', self.links],
+                ['joints', self.joints],
+                ['contacts', self.contacts],
+                ['xfrc', self.xfrc],
+                ['muscles', self.muscles],
             ]
             if data is not None
         }
 
     def plot(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Dict:
         """Plot"""
         plots = {}
@@ -241,37 +174,37 @@ class LinkSensorArray(SensorData, LinkSensorArrayCy):
     """Links array"""
 
     def __init__(
-        self,
-        array: NDARRAY_LINKS_ARRAY,
-        names: List[str],
+            self,
+            array: NDARRAY_LINKS_ARRAY,
+            names: List[str],
     ):
         super().__init__(array, names)
         self.masses = None
 
     @classmethod
     def from_dict(
-        cls,
-        dictionary: Dict,
+            cls,
+            dictionary: Dict,
     ):
         """Load data from dictionary"""
         links = super(cls, cls).from_dict(dictionary)
-        links.masses = dictionary["masses"]
+        links.masses = dictionary['masses']
         return links
 
     def to_dict(
-        self,
-        iteration: int = None,
+            self,
+            iteration: int = None,
     ):
         """Convert data to dictionary"""
         links = super().to_dict(iteration=iteration)
-        links["masses"] = self.masses
+        links['masses'] = self.masses
         return links
 
     @classmethod
     def from_names(
-        cls,
-        names: List[str],
-        n_iterations: int,
+            cls,
+            names: List[str],
+            n_iterations: int,
     ):
         """From names"""
         n_sensors = len(names)
@@ -284,10 +217,9 @@ class LinkSensorArray(SensorData, LinkSensorArrayCy):
 
     @classmethod
     def from_size(
-        cls,
-        n_links: int,
-        n_iterations: int,
-        names: List[str],
+            cls, n_links: int,
+            n_iterations: int,
+            names: List[str],
     ):
         """From size"""
         links = np.full(
@@ -299,10 +231,10 @@ class LinkSensorArray(SensorData, LinkSensorArrayCy):
 
     @classmethod
     def from_parameters(
-        cls,
-        n_iterations: int,
-        n_links: int,
-        names: List[str],
+            cls,
+            n_iterations: int,
+            n_links: int,
+            names: List[str],
     ):
         """From parameters"""
         return cls(
@@ -315,123 +247,87 @@ class LinkSensorArray(SensorData, LinkSensorArrayCy):
         )
 
     def com_position(
-        self,
-        iteration: int,
-        link_i: int,
+            self,
+            iteration: int,
+            link_i: int,
     ) -> NDARRAY_3_D:
         """CoM position of a link"""
-        return self.array[
-            iteration, link_i, sc.link_com_position_x : sc.link_com_position_z + 1
-        ]
+        return self.array[iteration, link_i, sc.link_com_position_x:sc.link_com_position_z+1]
 
     def com_orientation(
-        self,
-        iteration: int,
-        link_i: int,
+            self,
+            iteration: int,
+            link_i: int,
     ) -> NDARRAY_4_D:
         """CoM orientation of a link"""
-        return self.array[
-            iteration, link_i, sc.link_com_orientation_x : sc.link_com_orientation_w + 1
-        ]
+        return self.array[iteration, link_i, sc.link_com_orientation_x:sc.link_com_orientation_w+1]
 
     def com_positions(self) -> NDARRAY_XX3_D:
         """CoM position of a link"""
-        return self.array[:, :, sc.link_com_position_x : sc.link_com_position_z + 1]
+        return self.array[:, :, sc.link_com_position_x:sc.link_com_position_z+1]
 
     def urdf_position(
-        self,
-        iteration: int,
-        link_i: int,
+            self,
+            iteration: int,
+            link_i: int,
     ) -> NDARRAY_3_D:
         """URDF position of a link"""
-        return self.array[
-            iteration, link_i, sc.link_urdf_position_x : sc.link_urdf_position_z + 1
-        ]
+        return self.array[iteration, link_i, sc.link_urdf_position_x:sc.link_urdf_position_z+1]
 
     def urdf_positions(self) -> NDARRAY_XX3_D:
         """URDF position of a link"""
-        return self.array[:, :, sc.link_urdf_position_x : sc.link_urdf_position_z + 1]
+        return self.array[:, :, sc.link_urdf_position_x:sc.link_urdf_position_z+1]
 
     def urdf_orientation(
-        self,
-        iteration: int,
-        link_i: int,
+            self,
+            iteration: int,
+            link_i: int,
     ) -> NDARRAY_4_D:
         """URDF orientation of a link"""
-        return self.array[
-            iteration,
-            link_i,
-            sc.link_urdf_orientation_x : sc.link_urdf_orientation_w + 1,
-        ]
+        return self.array[iteration, link_i, sc.link_urdf_orientation_x:sc.link_urdf_orientation_w+1]
 
     def urdf_orientations(self) -> NDARRAY_XX4_D:
         """URDF orientation of a link"""
-        return self.array[
-            :, :, sc.link_urdf_orientation_x : sc.link_urdf_orientation_w + 1
-        ]
+        return self.array[:, :, sc.link_urdf_orientation_x:sc.link_urdf_orientation_w+1]
 
     def com_lin_velocity(
-        self,
-        iteration: int,
-        link_i: int,
+            self,
+            iteration: int,
+            link_i: int,
     ) -> NDARRAY_3_D:
         """CoM linear velocity of a link"""
-        return self.array[
-            iteration,
-            link_i,
-            sc.link_com_velocity_lin_x : sc.link_com_velocity_lin_z + 1,
-        ]
+        return self.array[iteration, link_i, sc.link_com_velocity_lin_x:sc.link_com_velocity_lin_z+1]
 
     def com_lin_velocities(self) -> NDARRAY_XX3_D:
         """CoM linear velocities"""
-        return self.array[
-            :, :, sc.link_com_velocity_lin_x : sc.link_com_velocity_lin_z + 1
-        ]
+        return self.array[:, :, sc.link_com_velocity_lin_x:sc.link_com_velocity_lin_z+1]
 
     def com_ang_velocity(
-        self,
-        iteration: int,
-        link_i: int,
+            self,
+            iteration: int,
+            link_i: int,
     ) -> NDARRAY_3_D:
         """CoM angular velocity of a link"""
-        return self.array[
-            iteration,
-            link_i,
-            sc.link_com_velocity_ang_x : sc.link_com_velocity_ang_z + 1,
-        ]
+        return self.array[iteration, link_i, sc.link_com_velocity_ang_x:sc.link_com_velocity_ang_z+1]
 
     def global_com_position(self, iteration: int):
         """Global CoM position"""
         total_mass = 0
         mass_position = np.zeros(3)
         for link_i, mass in enumerate(self.masses):
-            mass_position += mass * self.com_position(
+            mass_position += mass*self.com_position(
                 iteration=iteration,
                 link_i=link_i,
             )
             total_mass += mass
-        assert total_mass > 0, "No masses"
+        assert total_mass > 0, 'No masses'
         mass_position /= total_mass
         return mass_position
 
-    def global_com_velocity(self, iteration: int):
-        """Global CoM velocities"""
-        total_mass = 0
-        mass_vel = np.zeros(3)
-        for link_i, mass in enumerate(self.masses):
-            mass_vel += mass * self.com_lin_velocity(
-                iteration=iteration,
-                link_i=link_i,
-            )
-            total_mass += mass
-        assert total_mass > 0, "No masses"
-        mass_vel /= total_mass
-        return mass_vel
-
     def heading(
-        self,
-        iteration: int,
-        indices: List[int],
+            self,
+            iteration: int,
+            indices: List[int],
     ) -> float:
         """Heading"""
         n_indices = len(indices)
@@ -446,426 +342,92 @@ class LinkSensorArray(SensorData, LinkSensorArrayCy):
                 out=ori,
             )
             link_orientation[link_idx] = ori[2]
-        return (
-            circmean(
-                samples=link_orientation,
-                low=-np.pi,
-                high=np.pi,
-            )
-            if n_indices > 1
-            else link_orientation[0]
-        )
+        return circmean(
+            samples=link_orientation,
+            low=-np.pi,
+            high=np.pi,
+        ) if n_indices > 1 else link_orientation[0]
 
-    def plots(
-        self,
-        times: NDARRAY_V1,
-        arena: str,  # ["sim", "real", None]
+    def plot(
+            self,
+            times: NDARRAY_V1,
     ) -> Dict:
         """Plot"""
         return {
-            "Link positions": self.plot_base_position(times, arena, xaxis=0, yaxis=1),
-            "Head and tail position": self.plot_base_position(
-                times, arena, xaxis=0, yaxis=1, links=[0, 10]
-            ),
-            "Link speeds": self.plot_base_velocity(times),
-            "Heading": self.plot_heading(times),
-            "Global COM position": self.plot_global_com_positions(times, arena),
-            "Global COM speed": self.plot_global_com_speed(times),
-            "Time snapshots": self.plot_time_snapshots_1(),
+            'base_position': self.plot_base_position(times, xaxis=0, yaxis=1),
+            'base_velocity': self.plot_base_velocity(times),
+            'heading': self.plot_heading(times),
         }
 
-    def com_average_speed(
-        self,
-    ) -> float:
-        """Average speed"""
-        number_sim_iterations = self.array.shape[0]
-        global_com_speed = []
-        for i in range(0, number_sim_iterations - 2):
-            global_com_speed.append(
-                np.linalg.norm(self.global_com_velocity(iteration=i))
-            )
-        return np.mean(global_com_speed)
-
-    def com_average_speed_noTransient(
-        self,
-    ) -> float:
-        """Average speed"""
-        number_sim_iterations = self.array.shape[0]
-        global_com_speed = []
-        transient = conf.CONF["testing_transient"] / conf.CONF["config"]["timestep"]
-        for i in range(int(transient), number_sim_iterations - 2):
-            global_com_speed.append(
-                np.linalg.norm(self.global_com_velocity(iteration=i))
-            )
-        return np.mean(global_com_speed)
-
     def plot_base_position(
-        self,
-        times: NDARRAY_V1,
-        arena: str,
-        xaxis: int = 0,
-        yaxis: int = 1,
-        links=None,
+            self,
+            times: NDARRAY_V1,
+            xaxis: int = 0,
+            yaxis: int = 1,
     ) -> Figure:
         """Plot"""
-        import random
-
-        fig = plt.figure(
-            f"Links position {random.randint(0, 100)}"
-        )  # randint assures that independent figures are created on multiple function calls
-        min_x = 0.0
-        max_x = 0.0
-        min_y = 0.0
-        max_y = 0.0
-        if links is None:
-            _iterable = range(self.size(1))
-        else:
-            _iterable = links
-        for link_i in _iterable:
-            data = np.asarray(self.urdf_positions())[: len(times), link_i]
+        fig = plt.figure('Links position')
+        for link_i in range(self.size(1)):
+            data = np.asarray(self.urdf_positions())[:len(times), link_i]
             plt.plot(
                 data[:, xaxis],
                 data[:, yaxis],
-                label=f"Link_{link_i}",
+                label=f'Link_{link_i}',
             )
-            # Update axis boundaries
-            if min(data[:, xaxis]) < min_x:
-                min_x = min(data[:, xaxis])
-            if min(data[:, yaxis]) < min_y:
-                min_y = min(data[:, yaxis])
-            if max(data[:, xaxis]) > max_x:
-                max_x = max(data[:, xaxis])
-            if max(data[:, yaxis]) > max_y:
-                max_y = max(data[:, yaxis])
-
-        plt.legend(
-            bbox_to_anchor=(1.05, 1),
-            borderaxespad=0,
-        )
-
-        # add pegs
-        pegs = self.plot_get_pegs(
-            min_x - PLOT_ARENA_BORDER,
-            max_x + PLOT_ARENA_BORDER,
-            min_y - PLOT_ARENA_BORDER,
-            max_y + PLOT_ARENA_BORDER,
-            arena,
-        )
-
-        if pegs:
-            circles = matplotlib.collections.PatchCollection(pegs)
-            plt.gca().add_collection(circles)
-
-        plt.xlabel("Position [m]")
-        plt.ylabel("Position [m]")
-        plt.axis("equal")
+        plt.legend()
+        plt.xlabel('Position [m]')
+        plt.ylabel('Position [m]')
+        plt.axis('equal')
         plt.grid(True)
         return fig
 
     def plot_base_velocity(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot"""
-        fig = plt.figure("Links velocities")
+        fig = plt.figure('Links velocities')
         for link_i in range(self.size(1)):
-            data = np.asarray(self.com_lin_velocities())[: len(times), link_i]
+            data = np.asarray(self.com_lin_velocities())[:len(times), link_i]
             plt.plot(
                 times,
                 np.linalg.norm(data, axis=-1),
-                label=f"Link_{link_i}",
+                label=f'Link_{link_i}',
             )
-        plt.legend(
-            bbox_to_anchor=(1.05, 1),
-            borderaxespad=0,
-        )
-        # plt.tight_layout()
-        plt.xlabel("Time [s]")
-        plt.ylabel("Velocity [m/s]")
+        plt.legend()
+        plt.xlabel('Time [s]')
+        plt.ylabel('Velocity [m/s]')
         plt.grid(True)
         return fig
 
     def plot_heading(
-        self,
-        times: NDARRAY_V1,
-        indices: List[int] = None,
+            self,
+            times: NDARRAY_V1,
+            indices: List[int] = None,
     ) -> Figure:
         """Plot"""
         if indices is None:
             indices = [0]
-        fig = plt.figure("Heading")
-        plt.plot(
-            times,
-            [self.heading(i, indices) for i in range(0, len(times))],
-        )
-        plt.legend()
-        plt.xlabel("Time [s]")
-        plt.ylabel("Heading [rad]")
-        plt.grid(True)
-        return fig
-
-    def plot_get_pegs(self, x_min, x_max, y_min, y_max, arena):
-        arena_geometry = {}
-        if arena == "sim":
-            arena_geometry = simulation_arena_geom
-        elif arena == "real":
-            arena_geometry = real_arena_geom
-        elif arena == None:
-            return
-        else:
-            raise ValueError("Unkown arena type")
-
-        pegs = []
-        for i in arena_geometry["pegs"]:
-            # if robot was close to peg
-            if i[0] > x_min and i[0] < x_max and i[1] > y_min and i[1] < y_max:
-                pegs.append(
-                    plt.Circle(
-                        (i[0], i[1]),
-                        radius=arena_geometry["radius"],
-                        linewidth=0,
-                        color="g",
-                    )
-                )
-        return pegs
-
-    def plot_time_snapshots_1(
-        self,
-        time_spacing: int = 40,  # time spacing between snapshots in seconds [s]
-    ) -> Figure:
-        fig = plt.figure("2D Snapshot of robot position")
-        number_sim_iterations = self.array.shape[0]
-        number_colors = len(gradient_2) - 1  # number of colors available in gradient_2
-        y_max = 0.0
-        y_min = 0.0
-
-        for i, timestep in enumerate(range(0, number_sim_iterations - 1, time_spacing)):
-            position_segments = np.array(
-                [
-                    np.array(self.com_position(timestep, segment))[0:2]
-                    for segment in range(11)
-                ]
-            )
-            # com_position = np.array(self.global_com_position(iteration=timestep))[0:2]
-            plt.plot(
-                position_segments[:, 0],
-                position_segments[:, 1],
-                color=gradient_2[i % number_colors],
-                alpha=0.5,
-            )
-            # plt.plot(
-            #     com_position[0],
-            #     com_position[1],
-            #     marker="o",
-            #     markersize=3,
-            #     color="red",
-            # )
-            if np.max(position_segments[:, 1]) > y_max:
-                y_max = np.max(position_segments[:, 1])
-            if np.min(position_segments[:, 1]) < y_min:
-                y_min = np.min(position_segments[:, 1])
-
-        plt.xlabel("x position [m]")
-        plt.ylabel("y position [m]")
-        plt.ylim(y_min - 0.2, y_max + 0.2)
-        ax = plt.gca()
-        ax.set_aspect("equal", adjustable="box")
-        plt.grid(True)
-
-        # TODO add pegs
-
-        return fig
-
-    def plot_global_com_positions(
-        self,
-        times: NDARRAY_V1,
-        arena: str,
-    ) -> Figure:
-        fig = plt.figure("Global CoM positions 2D")
-        global_com_position_x = []
-        global_com_position_y = []
-
-        for i in range(0, len(times)):
-            global_com_position_x.append(self.global_com_position(iteration=i)[0])
-            global_com_position_y.append(self.global_com_position(iteration=i)[1])
-        plt.plot(
-            global_com_position_x,
-            global_com_position_y,
-        )
-        # mark start red and end blue
-        plt.plot(
-            global_com_position_x[0],
-            global_com_position_y[0],
-            marker="o",
-            markersize=5,
-            color="red",
-        )
-        plt.plot(
-            global_com_position_x[-1],
-            global_com_position_y[-1],
-            marker="o",
-            markersize=5,
-            color="blue",
-        )
-
-        # plot pegs
-        pegs = self.plot_get_pegs(
-            min(global_com_position_x) - PLOT_ARENA_BORDER,
-            max(global_com_position_x) + PLOT_ARENA_BORDER,
-            min(global_com_position_y) - PLOT_ARENA_BORDER,
-            max(global_com_position_y) + PLOT_ARENA_BORDER,
-            arena,
-        )
-        if pegs:
-            circles = matplotlib.collections.PatchCollection(pegs)
-            plt.gca().add_collection(circles)
-
-        plt.text(global_com_position_x[0], global_com_position_y[0], "start")
-        plt.text(global_com_position_x[-1], global_com_position_y[-1], "end")
-        plt.legend()
-        plt.xlabel("x position [m]")
-        plt.ylabel("y position [m]")
-        plt.axis("equal")
-        plt.grid(True)
-        return fig
-
-    def plot_global_com_speed(
-        self,
-        times: NDARRAY_V1,
-    ) -> Figure:
-        global_com_speed = []
-        global_com_speed_x = []
-        global_com_speed_y = []
-        fig = plt.figure("Global CoM velocities")
-        for i in range(0, len(times)):
-            global_com_speed.append(
-                np.linalg.norm(self.global_com_velocity(iteration=i))
-            )
-            global_com_speed_x.append(self.global_com_velocity(iteration=i)[0])
-            global_com_speed_y.append(self.global_com_velocity(iteration=i)[1])
+        fig = plt.figure('Heading')
         plt.plot(
             times,
-            global_com_speed,
-            label="total",
-        )
-        plt.plot(
-            times,
-            global_com_speed_x,
-            label="x",
-        )
-        plt.plot(
-            times,
-            global_com_speed_y,
-            label="y",
+            [self.heading(i, indices) for i, _ in enumerate(times)],
         )
         plt.legend()
-        plt.xlabel("Time [s]")
-        plt.ylabel("Global COM speed [m/s]")
+        plt.xlabel('Time [s]')
+        plt.ylabel('Heading [rad]')
         plt.grid(True)
         return fig
 
-    # calculate total distance travelled in one axis direction
-    def com_distance_travelled_in_axis(
-        self,
-        abs_: bool = False,
-        axis: int = 0,  # x = 0, y = 1, z = 2
-    ):
-        number_sim_iterations = self.array.shape[0]
-        x_start = self.global_com_position(0)[axis]
-        x_end = self.global_com_position(number_sim_iterations - 2)[
-            axis
-        ]  # -2 required because last simulation steps yields 0.0. Reason unkown.
-        if abs_:
-            return abs(x_end - x_start)
-        else:
-            return x_end - x_start
-
-    # calculate  the total length of the path taken by the com in swimming plane / x-y-plane
-    # returns abs value
-    def com_path_length_in_swimming_plane(self):
-        # TODO use np.diff instead
-        total_distance = 0.0
-        number_sim_iterations = self.array.shape[0]
-        for i in range(
-            1, number_sim_iterations - 1
-        ):  # - 1 required because last simulation step gives 0.0. Reason unkown.
-            prev = self.global_com_position(i - 1)[0:2]
-            curr = self.global_com_position(i)[0:2]
-            total_distance += np.linalg.norm(curr - prev)
-        return total_distance
-
-    def com_path_length_in_swimming_plane_noTransient(self):
-        # TODO use np.diff instead
-        total_distance = 0.0
-        number_sim_iterations = self.array.shape[0]
-        transient = conf.CONF["testing_transient"] / conf.CONF["config"]["timestep"]
-        for i in range(
-            int(transient), number_sim_iterations - 1
-        ):  # - 1 required because last simulation step gives 0.0. Reason unkown.
-            prev = self.global_com_position(i - 1)[0:2]
-            curr = self.global_com_position(i)[0:2]
-            total_distance += np.linalg.norm(curr - prev)
-        return total_distance
-
-    def urdf_path_length_in_swimming_plane(self, link_i=0):
-        # TODO use np.diff instead
-        total_distance = 0.0
-        number_sim_iterations = self.array.shape[0]
-        for i in range(
-            1, number_sim_iterations - 1
-        ):  # - 1 required because last simulation step gives 0.0. Reason unkown.
-            prev = self.urdf_position(iteration=i - 1, link_i=link_i)[0:2]
-            curr = self.urdf_position(iteration=i, link_i=link_i)[0:2]
-            total_distance += np.linalg.norm(np.array(curr) - np.array(prev))
-        return total_distance
-
-    # calculate total euclidean distance travelled by com between start and end
-    def com_distance_travelled_start_end(self):
-        number_sim_iterations = self.array.shape[0]
-        start = self.global_com_position(0)[0:2]
-        end = self.global_com_position(number_sim_iterations - 2)[
-            0:2
-        ]  # - 2 required because last simulation step gives 0.0. Reason unkown.
-        return np.linalg.norm(start - end)
-
-    # calculate total euclidean distance travelled by com between start and end
-    def com_distance_travelled_start_end_noTransient(self):
-        number_sim_iterations = self.array.shape[0]
-        transient = conf.CONF["testing_transient"] / conf.CONF["config"]["timestep"]
-        start = self.global_com_position(int(transient))[0:2]
-        end = self.global_com_position(number_sim_iterations - 2)[
-            0:2
-        ]  # - 2 required because last simulation step gives 0.0. Reason unkown.
-        return np.linalg.norm(start - end)
-
-    def performance_metrics(
-        self,
-    ) -> dict:
-        return {
-            # "com_distance_travelled_in_axis (x-axis): ": self.com_distance_travelled_in_axis(),
-            "2_com_path_length_in_swimming_plane": self.com_path_length_in_swimming_plane(),
-            "2_com_path_length_in_swimming_plane_noTransient": self.com_path_length_in_swimming_plane_noTransient(),
-            "2_com_distance_travelled_start_end": self.com_distance_travelled_start_end(),
-            "2_com_distance_travelled_start_end_noTransient": self.com_distance_travelled_start_end_noTransient(),
-            "2_com_average_speed": self.com_average_speed(),
-            "2_com_average_speed_noTransient": self.com_average_speed_noTransient(),
-            "2_link_0_path_length_in_swimming_plane (head)": self.urdf_path_length_in_swimming_plane(
-                link_i=0
-            ),
-            "2_link_10_path_length_in_swimming_plane (tail)": self.urdf_path_length_in_swimming_plane(
-                link_i=10
-            ),
-        }
-
 
 class JointSensorArray(SensorData, JointSensorArrayCy):
     """Joint sensor array"""
 
     @classmethod
     def from_names(
-        cls,
-        names: List[str],
-        n_iterations: int,
+            cls,
+            names: List[str],
+            n_iterations: int,
     ):
         """From names"""
         n_sensors = len(names)
@@ -878,10 +440,10 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
 
     @classmethod
     def from_size(
-        cls,
-        n_joints: int,
-        n_iterations: int,
-        names: List[str],
+            cls,
+            n_joints: int,
+            n_iterations: int,
+            names: List[str],
     ):
         """From size"""
         joints = np.full(
@@ -893,10 +455,10 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
 
     @classmethod
     def from_parameters(
-        cls,
-        n_iterations: int,
-        n_joints: int,
-        names: List[str],
+            cls,
+            n_iterations: int,
+            n_joints: int,
+            names: List[str],
     ):
         """From parameters"""
         return cls(
@@ -909,16 +471,16 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         )
 
     def position(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Joint position"""
         return self.array[iteration, joint_i, sc.joint_position]
 
     def positions(
-        self,
-        iteration: int,
+            self,
+            iteration: int,
     ) -> NDARRAY_V1_D:
         """Joints positions"""
         return self.array[iteration, :, sc.joint_position]
@@ -927,103 +489,17 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         """Joints positions"""
         return self.array[:, :, sc.joint_position]
 
-    def position_sum_all(self) -> NDARRAY_V1_D:
-        # TODO provide argument to return absolute or non-absolute sum
-        """Joints positions"""
-        return np.sum(self.array[:, :, sc.joint_position], axis=1)
-
-    def get_joint_position_differences(self):
-        return np.round(np.diff(self.positions_all(), axis=1), decimals=2)
-
-    def plot_joint_position_differences(self, times: NDARRAY_V1) -> Figure:
-        fig = plt.figure(f"Joint position lags")
-        joint_pos_diff = self.get_joint_position_differences()
-        for i, data in enumerate(np.transpose(np.array(joint_pos_diff))):
-            plt.plot(
-                times,
-                data[: len(times)],
-                label=f"Joint {i} - {i+1}",  # color=gradient[i],
-            )
-        plt.legend(
-            bbox_to_anchor=(1.05, 1),
-            borderaxespad=0,
-        )
-        plt.xlabel("Times [s]")
-        plt.ylabel("Position differences [rad]")
-        plt.grid(True)
-        return fig
-
-    def sum_position_diff_joints(
-        self,
-    ) -> NDARRAY_V1_D:
-        """get total change of joint position over simulation time"""
-        number_sim_iterations = self.array.shape[0]
-        # the sum calculation should only go until (number_sim_iterations - 2)
-        diff = np.diff(self.positions_all(), axis=0)[0 : number_sim_iterations - 2]
-        return np.sum(np.abs(diff), axis=0)
-
-    def sum_power_joints_timestep(self):
-        return np.multiply(np.abs(self.active_torques()), np.abs(self.velocities_all()))
-
-    def sum_energy_joints_timestep(self):
-        """get power consumption for one joint over simulation time; returns array"""
-        return np.multiply(
-            self.sum_power_joints_timestep(), conf.CONF["config"]["timestep"]
-        )
-
-    def sum_energy_joints_timestep_noTransient(self):
-        """get power consumption for one joint over simulation time; returns array"""
-        transient = conf.CONF["testing_transient"] / conf.CONF["config"]["timestep"]
-        return np.multiply(
-            self.sum_power_joints_timestep()[int(transient) :],
-            conf.CONF["config"]["timestep"],
-        )
-
-    # def sum_virtual_power_joints(self):
-    #     """get power consumption for one joint over simulation time; returns array"""
-    #     power_timesteps = np.multiply(
-    #         np.abs(self.cmd_torques()), np.abs(self.velocities_all())
-    #     )
-    #     power_timesteps = np.multiply(power_timesteps, conf.CONF["config"]["timestep"])
-    #     power_total = np.sum(power_timesteps, axis=0)
-    #     return power_total
-
-    def performance_metrics(self):
-        return {
-            # "joints_sum_position_differences (abs): ": np.sum(
-            #     self.sum_position_diff_joints()
-            # ),
-            "3_joints_total_energy (active torques)": np.sum(
-                self.sum_energy_joints_timestep(), axis=None
-            ),
-            "3_joints_total_energy_noTransient (active torques)": np.sum(
-                self.sum_energy_joints_timestep_noTransient(), axis=None
-            ),
-            # "joint_sum_virtual_power (cmd torques): ": np.sum(
-            #     self.sum_virtual_power_joints()
-            # ),
-            "3_joints_energy_per_second (active torques)": np.sum(
-                self.sum_energy_joints_timestep(), axis=None
-            )
-            / conf.CONF["simulation_time_testing"],
-            # "joint_sum_virtual_power per second (cmd torques): ": np.sum(
-            #     self.sum_virtual_power_joints() / conf.CONF["config"]["simulation_time"]
-            # ),
-            # "joint_sum_active_torques (abs): ": np.sum(np.abs(self.active_torques())),
-            # "joint_sum_cmd_torques (abs): ": np.sum(np.abs(self.cmd_torques())),
-        }
-
     def velocity(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Joint velocity"""
         return self.array[iteration, joint_i, sc.joint_velocity]
 
     def velocities(
-        self,
-        iteration: int,
+            self,
+            iteration: int,
     ) -> NDARRAY_V1_D:
         """Joints velocities"""
         return self.array[iteration, :, sc.joint_velocity]
@@ -1033,16 +509,16 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_velocity]
 
     def motor_torque(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Joint torque"""
         return self.array[iteration, joint_i, sc.joint_torque]
 
     def motor_torques(
-        self,
-        iteration: int,
+            self,
+            iteration: int,
     ) -> NDARRAY_V1_D:
         """Joint torques"""
         return self.array[iteration, :, sc.joint_torque]
@@ -1052,33 +528,33 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_torque]
 
     def force(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> NDARRAY_3_D:
         """Joint force"""
-        return self.array[iteration, joint_i, sc.joint_force_x : sc.joint_force_z + 1]
+        return self.array[iteration, joint_i, sc.joint_force_x:sc.joint_force_z+1]
 
     def forces_all(self) -> NDARRAY_XX3_D:
         """Joints forces"""
-        return self.array[:, :, sc.joint_force_x : sc.joint_force_z + 1]
+        return self.array[:, :, sc.joint_force_x:sc.joint_force_z+1]
 
     def torque(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> NDARRAY_3_D:
         """Joint torque"""
-        return self.array[iteration, joint_i, sc.joint_torque_x : sc.joint_torque_z + 1]
+        return self.array[iteration, joint_i, sc.joint_torque_x:sc.joint_torque_z+1]
 
     def torques_all(self) -> NDARRAY_XX3_D:
         """Joints torques"""
-        return self.array[:, :, sc.joint_torque_x : sc.joint_torque_z + 1]
+        return self.array[:, :, sc.joint_torque_x:sc.joint_torque_z+1]
 
     def cmd_position(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Joint position"""
         return self.array[iteration, joint_i, sc.joint_cmd_position]
@@ -1088,9 +564,9 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_cmd_position]
 
     def cmd_velocity(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Joint velocity"""
         return self.array[iteration, joint_i, sc.joint_cmd_velocity]
@@ -1100,9 +576,9 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_cmd_velocity]
 
     def cmd_torque(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Joint torque"""
         return self.array[iteration, joint_i, sc.joint_cmd_torque]
@@ -1112,9 +588,9 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_cmd_torque]
 
     def active(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Active torque"""
         return self.array[iteration, joint_i, sc.joint_torque_active]
@@ -1124,9 +600,9 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_torque_active]
 
     def spring(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """Passive spring torque"""
         return self.array[iteration, joint_i, sc.joint_torque_stiffness]
@@ -1136,9 +612,9 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_torque_stiffness]
 
     def damping(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """passive damping torque"""
         return self.array[iteration, joint_i, sc.joint_torque_damping]
@@ -1148,9 +624,9 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return self.array[:, :, sc.joint_torque_damping]
 
     def friction(
-        self,
-        iteration: int,
-        joint_i: int,
+            self,
+            iteration: int,
+            joint_i: int,
     ) -> float:
         """passive friction torque"""
         return self.array[iteration, joint_i, sc.joint_torque_friction]
@@ -1159,120 +635,105 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         """Friction torques"""
         return self.array[:, :, sc.joint_torque_friction]
 
-    def plots(
-        self,
-        times: NDARRAY_V1,
-    ) -> Dict:
-        return {
-            # "joints_positions": self.plot_positions(times),  #!=0
-            "joints_velocities": self.plot_velocities(times),  #!=0
-            "joints_cmd_torques": self.plot_cmd_torques(times),  #!=0
-            "joints_active_torques": self.plot_active_torques(times),  #!=0
-            "joint_positions_sum_all": self.plot_position_sum_all(times),
-            "joint_position_differences": self.plot_joint_position_differences(times),
-            "joint_powers": self.plot_power_all(times),
-        }
+    def mechanical_power(self):
+        """Compute mechanical power"""
+        return np.array(self.cmd_torques())*np.array(self.velocities_all())
+
+    def mechanical_power_active(self):
+        """Compute active mechanical power"""
+        return np.array(self.active_torques())*np.array(self.velocities_all())
+
+    def limit_force(
+            self,
+            iteration: int,
+            joint_i: int,
+    ) -> NDARRAY_3_D:
+        """Joint limit force"""
+        return self.array[iteration, joint_i, sc.joint_limit_force]
+
+    def limit_forces_all(self) -> NDARRAY_XX3_D:
+        """Joints limits forces"""
+        return self.array[:, :, sc.joint_limit_force]
 
     def plot(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Dict:
         """Plot"""
         t_init = times[:50]
         return {
-            "joints_positions": self.plot_positions(times),  #!=0
-            "joints_velocities": self.plot_velocities(times),  #!=0
-            "joints_motor_torques": self.plot_motor_torques(times),
-            "joints_forces": self.plot_forces(times),
-            "joints_torques": self.plot_torques(times),
-            "joints_cmd_positions": self.plot_cmd_positions(times),
-            "joints_cmd_velocities": self.plot_cmd_velocities(times),
-            "joints_cmd_torques": self.plot_cmd_torques(times),  #!=0
-            "joints_active_torques": self.plot_active_torques(times),  #!=0
-            "joints_spring_torques": self.plot_spring_torques(times),
-            "joints_damping_torques": self.plot_damping_torques(times),
-            "joints_friction_torques": self.plot_friction_torques(times),
-            "joints_ti_positions": self.plot_positions(t_init, " init"),
-            "joints_ti_velocities": self.plot_velocities(t_init, " init"),
-            "joints_ti_motor_torques": self.plot_motor_torques(t_init, " init"),
-            "joints_ti_cmd_positions": self.plot_cmd_positions(t_init, " init"),
-            "joints_ti_cmd_velocities": self.plot_cmd_velocities(t_init, " init"),
-            "joints_ti_cmd_torques": self.plot_cmd_torques(t_init, " init"),
-            "joints_ti_active": self.plot_active_torques(t_init, " init"),
-            "joints_ti_spring": self.plot_spring_torques(t_init, " init"),
-            "joints_ti_damping": self.plot_damping_torques(t_init, " init"),
-            "joints_ti_friction": self.plot_friction_torques(t_init, " init"),
+            'joints_positions': self.plot_positions(times),
+            'joints_velocities': self.plot_velocities(times),
+            'joints_motor_torques': self.plot_motor_torques(times),
+            'joints_forces': self.plot_forces(times),
+            'joints_torques': self.plot_torques(times),
+            'joints_cmd_positions': self.plot_cmd_positions(times),
+            'joints_cmd_velocities': self.plot_cmd_velocities(times),
+            'joints_cmd_torques': self.plot_cmd_torques(times),
+            'joints_active_torques': self.plot_active_torques(times),
+            'joints_spring_torques': self.plot_spring_torques(times),
+            'joints_damping_torques': self.plot_damping_torques(times),
+            'joints_friction_torques': self.plot_friction_torques(times),
+            'joints_ti_positions': self.plot_positions(t_init, ' init'),
+            'joints_ti_velocities': self.plot_velocities(t_init, ' init'),
+            'joints_ti_motor_torques': self.plot_motor_torques(t_init, ' init'),
+            'joints_ti_cmd_positions': self.plot_cmd_positions(t_init, ' init'),
+            'joints_ti_cmd_velocities': self.plot_cmd_velocities(t_init, ' init'),
+            'joints_ti_cmd_torques': self.plot_cmd_torques(t_init, ' init'),
+            'joints_ti_active': self.plot_active_torques(t_init, ' init'),
+            'joints_ti_spring': self.plot_spring_torques(t_init, ' init'),
+            'joints_ti_damping': self.plot_damping_torques(t_init, ' init'),
+            'joints_ti_friction': self.plot_friction_torques(t_init, ' init'),
         }
 
     def plot_data(
-        self,
-        times: NDARRAY_V1,
-        data: NDARRAY_V2_D,
-        joint_i: int,
+            self,
+            times: NDARRAY_V1,
+            data: NDARRAY_V2_D,
+            joint_i: int,
     ):
         """Plot data"""
         _data = np.asarray(data)
-        if len(_data.shape) == 1:
-            plt.plot(
-                times,
-                _data[: len(times)],
-            )
-        else:
-            if joint_i < self.size(1):
-                label = self.names[joint_i]
-            else:
-                label = "sum"
-            plt.plot(
-                times,
-                _data[: len(times), joint_i],
-                label=label,
-                color=gradient_1[
-                    joint_i
-                ],  # set color gradient; only works for 10 joints, 11th color is for sum
-            )
+        plt.plot(times, _data[:len(times), joint_i], label=self.names[joint_i])
 
     @staticmethod
     def plot_end(
-        ylabel: str,
-        n_labels: int,
-        max_labels_per_row: int = 20,
+            ylabel: str,
+            n_labels: int,
+            max_labels_per_row: int = 20,
     ):
         """plot_end"""
         plt.legend(
             bbox_to_anchor=(1.05, 1),
+            loc='upper left',
             borderaxespad=0,
-            ncol=n_labels // max_labels_per_row + 1,
+            ncol=n_labels//max_labels_per_row+1,
         )
-        plt.xlabel("Time [s]")
+        plt.xlabel('Times [s]')
         plt.ylabel(ylabel)
         plt.grid(True)
-        # plt.tight_layout()
 
     def plot_generic(
-        self,
-        times: NDARRAY_V1,
-        data: NDARRAY_V2_D,
-        title: str,
-        ylabel: str,
+            self,
+            times: NDARRAY_V1,
+            data: NDARRAY_V2_D,
+            title: str,
+            ylabel: str,
     ) -> Figure:
         """Plot joint sensor"""
         fig = plt.figure(title)
         n_joints = self.size(1)
-        # if additional sum of all joints is needed, add 1
-        if len(data.shape) > 1:
-            if data.shape[1] > n_joints:
-                n_joints += 1
         for joint_i in range(n_joints):
             self.plot_data(times=times, data=data, joint_i=joint_i)
         self.plot_end(ylabel=ylabel, n_labels=n_joints)
         return fig
 
     def plot_generic_3(
-        self,
-        times: NDARRAY_V1,
-        data: NDARRAY_V2_D,
-        title: str,
-        ylabel: str,
+            self,
+            times: NDARRAY_V1,
+            data: NDARRAY_V2_D,
+            title: str,
+            ylabel: str,
     ) -> Figure:
         """Plot ground reaction forces"""
         fig = plt.figure(title)
@@ -1284,212 +745,159 @@ class JointSensorArray(SensorData, JointSensorArrayCy):
         return fig
 
     def plot_positions(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
+        """Plot ground reaction forces"""
         return self.plot_generic(
             times=times,
             data=self.positions_all(),
-            title=f"Joints positions{suffix}",
-            ylabel="Joint position [rad]",
-        )
-
-    def plot_position_sum_all(
-        self,
-        times: NDARRAY_V1,
-        plot_individual_joint_positions=True,
-    ):
-        data = self.position_sum_all()
-        if plot_individual_joint_positions:
-            data = np.column_stack((self.positions_all(), data))
-
-        return self.plot_generic(
-            times=times,
-            data=data,
-            title=f"Sum of joints positions",
-            ylabel="Sum of joint position [rad]",
-        )
-
-    def plot_power_all(
-        self,
-        times: NDARRAY_V1,
-        plot_sum=True,
-    ):
-        data = self.sum_power_joints_timestep()
-
-        if plot_sum:
-            data = np.column_stack(
-                (data, np.sum(self.sum_power_joints_timestep(), axis=1))
-            )
-
-        return self.plot_generic(
-            times=times,
-            data=data,
-            title=f"Power of joints",
-            ylabel="Power [W]",
+            title=f'Joints positions{suffix}',
+            ylabel='Joint position [rad]',
         )
 
     def plot_velocities(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
-        plot_sum=True,
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
-        data = self.velocities_all()
-
-        if plot_sum:
-            data = np.column_stack((data, np.sum(self.velocities_all(), axis=1)))
-
+        """Plot ground reaction forces"""
         return self.plot_generic(
             times=times,
-            data=data,
-            title=f"Joints velocities{suffix}",
-            ylabel="Joint velocity [rad/s]",
+            data=self.velocities_all(),
+            title=f'Joints velocities{suffix}',
+            ylabel='Joint velocity [rad/s]',
         )
 
     def plot_motor_torques(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints motor torques"""
         return self.plot_generic(
             times=times,
             data=self.motor_torques_all(),
-            title=f"Joints motor torques{suffix}",
-            ylabel="Joint torque [Nm]",
+            title=f'Joints motor torques{suffix}',
+            ylabel='Joint torque [Nm]',
         )
 
     def plot_forces(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot ground reaction forces"""
         return self.plot_generic_3(
             times=times,
             data=self.forces_all(),
-            title=f"Joints forces{suffix}",
-            ylabel="Joint force [N]",
+            title=f'Joints forces{suffix}',
+            ylabel='Joint force [N]',
         )
 
     def plot_torques(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot ground reaction torques"""
         return self.plot_generic_3(
             times=times,
             data=self.torques_all(),
-            title=f"Joints torques{suffix}",
-            ylabel="Joint torque [Nm]",
+            title=f'Joints torques{suffix}',
+            ylabel='Joint torque [Nm]',
         )
 
     def plot_cmd_positions(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints command positions"""
         return self.plot_generic(
             times=times,
             data=self.cmd_positions(),
-            title=f"Joints command positions{suffix}",
-            ylabel="Joint position [rad]",
+            title=f'Joints command positions{suffix}',
+            ylabel='Joint position [rad]',
         )
 
     def plot_cmd_velocities(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints command velocities"""
         return self.plot_generic(
             times=times,
             data=self.cmd_velocities(),
-            title=f"Joints command velocities{suffix}",
-            ylabel="Joint velocity [rad/s]",
+            title=f'Joints command velocities{suffix}',
+            ylabel='Joint velocity [rad/s]',
         )
 
     def plot_cmd_torques(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
-        plot_sum=True,
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints command torques"""
-
-        data = self.cmd_torques()
-
-        if plot_sum:
-            data = np.column_stack((data, np.sum(self.cmd_torques(), axis=1)))
-
         return self.plot_generic(
             times=times,
-            data=data,
-            title=f"Joints command torques{suffix}",
-            ylabel="Joint torque [Nm]",
+            data=self.cmd_torques(),
+            title=f'Joints command torques{suffix}',
+            ylabel='Joint torque [Nm]',
         )
 
     def plot_active_torques(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
-        plot_sum=True,
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints active torques"""
-
-        data = self.active_torques()
-
-        if plot_sum:
-            data = np.column_stack((data, np.sum(self.active_torques(), axis=1)))
-
         return self.plot_generic(
             times=times,
-            data=data,
-            title=f"Joints active torques{suffix}",
-            ylabel="Joint torque [Nm]",
+            data=self.active_torques(),
+            title=f'Joints active torques{suffix}',
+            ylabel='Joint torque [Nm]',
         )
 
     def plot_spring_torques(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints spring torques"""
         return self.plot_generic(
             times=times,
             data=self.spring_torques(),
-            title=f"Joints stiffness torques{suffix}",
-            ylabel="Joint torque [Nm]",
+            title=f'Joints stiffness torques{suffix}',
+            ylabel='Joint torque [Nm]',
         )
 
     def plot_damping_torques(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints damping torques"""
         return self.plot_generic(
             times=times,
             data=self.damping_torques(),
-            title=f"Joints damping torques{suffix}",
-            ylabel="Joint torque [Nm]",
+            title=f'Joints damping torques{suffix}',
+            ylabel='Joint torque [Nm]',
         )
 
     def plot_friction_torques(
-        self,
-        times: NDARRAY_V1,
-        suffix: str = "",
+            self,
+            times: NDARRAY_V1,
+            suffix: str = '',
     ) -> Figure:
         """Plot joints friction torques"""
         return self.plot_generic(
             times=times,
             data=self.friction_torques(),
-            title=f"Joints friction torques{suffix}",
-            ylabel="Joint torque [Nm]",
+            title=f'Joints friction torques{suffix}',
+            ylabel='Joint torque [Nm]',
         )
 
 
@@ -1498,9 +906,9 @@ class ContactsArray(SensorData, ContactsArrayCy):
 
     @classmethod
     def from_names(
-        cls,
-        names: List[List[str]],
-        n_iterations: int,
+            cls,
+            names: List[List[str]],
+            n_iterations: int,
     ):
         """From names"""
         n_sensors = len(names)
@@ -1513,10 +921,10 @@ class ContactsArray(SensorData, ContactsArrayCy):
 
     @classmethod
     def from_parameters(
-        cls,
-        n_iterations: int,
-        n_contacts: int,
-        names: List[str],
+            cls,
+            n_iterations: int,
+            n_contacts: int,
+            names: List[str],
     ):
         """From parameters"""
         return cls(
@@ -1530,10 +938,10 @@ class ContactsArray(SensorData, ContactsArrayCy):
 
     @classmethod
     def from_size(
-        cls,
-        n_contacts: int,
-        n_iterations: int,
-        names: List[str],
+            cls,
+            n_contacts: int,
+            n_iterations: int,
+            names: List[str],
     ):
         """From size"""
         contacts = np.full(
@@ -1544,213 +952,188 @@ class ContactsArray(SensorData, ContactsArrayCy):
         return cls(contacts, names)
 
     def reaction(
-        self,
-        iteration: int,
-        sensor_i: int,
+            self,
+            iteration: int,
+            sensor_i: int,
     ) -> NDARRAY_3_D:
         """Reaction force"""
-        return self.array[
-            iteration, sensor_i, sc.contact_reaction_x : sc.contact_reaction_z + 1
-        ]
+        return self.array[iteration, sensor_i, sc.contact_reaction_x:sc.contact_reaction_z+1]
 
     def reaction_all(
-        self,
-        sensor_i: int,
+            self,
+            sensor_i: int,
     ) -> NDARRAY_X3_D:
         """Reaction forces"""
-        return self.array[
-            :, sensor_i, sc.contact_reaction_x : sc.contact_reaction_z + 1
-        ]
+        return self.array[:, sensor_i, sc.contact_reaction_x:sc.contact_reaction_z+1]
 
     def reactions(self) -> NDARRAY_XX3_D:
         """Reaction forces"""
-        return self.array[:, :, sc.contact_reaction_x : sc.contact_reaction_z + 1]
+        return self.array[:, :, sc.contact_reaction_x:sc.contact_reaction_z+1]
 
     def friction(
-        self,
-        iteration: int,
-        sensor_i: int,
+            self,
+            iteration: int,
+            sensor_i: int,
     ) -> NDARRAY_3_D:
         """Friction force"""
-        return self.array[
-            iteration, sensor_i, sc.contact_friction_x : sc.contact_friction_z + 1
-        ]
+        return self.array[iteration, sensor_i, sc.contact_friction_x:sc.contact_friction_z+1]
 
     def friction_all(
-        self,
-        sensor_i: int,
+            self,
+            sensor_i: int,
     ) -> NDARRAY_X3_D:
         """Friction forces"""
-        return self.array[
-            :, sensor_i, sc.contact_friction_x : sc.contact_friction_z + 1
-        ]
+        return self.array[:, sensor_i, sc.contact_friction_x:sc.contact_friction_z+1]
 
     def frictions(self) -> NDARRAY_XX3_D:
         """Friction forces"""
-        return self.array[:, :, sc.contact_friction_x : sc.contact_friction_z + 1]
+        return self.array[:, :, sc.contact_friction_x:sc.contact_friction_z+1]
 
     def total(
-        self,
-        iteration: int,
-        sensor_i: int,
+            self,
+            iteration: int,
+            sensor_i: int,
     ) -> NDARRAY_3_D:
         """Total force"""
-        return self.array[
-            iteration, sensor_i, sc.contact_total_x : sc.contact_total_z + 1
-        ]
+        return self.array[iteration, sensor_i, sc.contact_total_x:sc.contact_total_z+1]
 
     def total_all(
-        self,
-        sensor_i: int,
+            self,
+            sensor_i: int,
     ) -> NDARRAY_X3_D:
         """Total forces"""
-        return self.array[:, sensor_i, sc.contact_total_x : sc.contact_total_z + 1]
+        return self.array[:, sensor_i, sc.contact_total_x:sc.contact_total_z+1]
 
     def totals(self) -> NDARRAY_XX3_D:
         """Total forces"""
-        return self.array[:, :, sc.contact_total_x : sc.contact_total_z + 1]
+        return self.array[:, :, sc.contact_total_x:sc.contact_total_z+1]
 
     def position(
-        self,
-        iteration: int,
-        sensor_i: int,
+            self,
+            iteration: int,
+            sensor_i: int,
     ) -> NDARRAY_3_D:
         """Position"""
-        return self.array[
-            iteration, sensor_i, sc.contact_position_x : sc.contact_position_z + 1
-        ]
+        return self.array[iteration, sensor_i, sc.contact_position_x:sc.contact_position_z+1]
 
     def position_all(
-        self,
-        sensor_i: int,
+            self,
+            sensor_i: int,
     ) -> NDARRAY_X3_D:
         """Positions"""
-        return self.array[
-            :, sensor_i, sc.contact_position_x : sc.contact_position_z + 1
-        ]
+        return self.array[:, sensor_i, sc.contact_position_x:sc.contact_position_z+1]
 
     def plot(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Dict:
         """Plot"""
         plots = {}
-        plots["reaction_forces"] = self.plot_ground_reaction_forces(times)
+        plots['reaction_forces'] = self.plot_ground_reaction_forces(times)
         plots.update(self.plot_ground_reaction_forces_all(times))
-        plots["friction_forces"] = self.plot_friction_forces(times)
-        plots["friction_forces_x"] = self.plot_friction_forces_ori(times, ori=0)
-        plots["friction_forces_y"] = self.plot_friction_forces_ori(times, ori=1)
-        plots["friction_forces_z"] = self.plot_friction_forces_ori(times, ori=2)
-        plots["total_forces"] = self.plot_total_forces(times)
-        return plots
-
-    def plots(
-        self,
-        times: NDARRAY_V1,
-    ) -> Dict:
-        plots = {}
-        plots["total_forces"] = self.plot_total_forces(times)
+        plots['friction_forces'] = self.plot_friction_forces(times)
+        plots['friction_forces_x'] = self.plot_friction_forces_ori(times, ori=0)
+        plots['friction_forces_y'] = self.plot_friction_forces_ori(times, ori=1)
+        plots['friction_forces_z'] = self.plot_friction_forces_ori(times, ori=2)
+        plots['total_forces'] = self.plot_total_forces(times)
         return plots
 
     def plot_ground_reaction_forces(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot ground reaction forces"""
-        fig = plt.figure("Ground reaction forces")
+        fig = plt.figure('Ground reaction forces')
         for sensor_i in range(self.size(1)):
             data = np.asarray(self.reaction_all(sensor_i))
             plt.plot(
                 times,
-                np.linalg.norm(data, axis=-1)[: len(times)],
-                label=f"Leg_{sensor_i}",
+                np.linalg.norm(data, axis=-1)[:len(times)],
+                label=f'Leg_{sensor_i}',
             )
         plt.legend()
-        plt.xlabel("Times [s]")
-        plt.ylabel("Forces [N]")
+        plt.xlabel('Times [s]')
+        plt.ylabel('Forces [N]')
         plt.grid(True)
         return fig
 
     def plot_ground_reaction_forces_all(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot ground reaction forces"""
         figs = {}
         for sensor_i, name in enumerate(self.names):
             data = np.asarray(self.reaction_all(sensor_i))
-            title = f"Ground reaction forces {name}"
+            title = f'Ground reaction forces {name}'
             figs[title] = plt.figure(title)
-            for direction_i, direction in enumerate(["x", "y", "z"]):
+            for direction_i, direction in enumerate(['x', 'y', 'z']):
                 plt.plot(
                     times,
-                    data[: len(times), direction_i],
-                    label=f"{name}_{direction}",
+                    data[:len(times), direction_i],
+                    label=f'{name}_{direction}',
                 )
         plt.legend()
-        plt.xlabel("Times [s]")
-        plt.ylabel("Forces [N]")
+        plt.xlabel('Times [s]')
+        plt.ylabel('Forces [N]')
         plt.grid(True)
         return figs
 
     def plot_friction_forces(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot friction forces"""
-        fig = plt.figure("Friction forces")
+        fig = plt.figure('Friction forces')
         for sensor_i in range(self.size(1)):
             data = np.asarray(self.friction_all(sensor_i))
             plt.plot(
                 times,
-                np.linalg.norm(data, axis=-1)[: len(times)],
-                label=f"Leg_{sensor_i}",
+                np.linalg.norm(data, axis=-1)[:len(times)],
+                label=f'Leg_{sensor_i}',
             )
         plt.legend()
-        plt.xlabel("Times [s]")
-        plt.ylabel("Forces [N]")
+        plt.xlabel('Times [s]')
+        plt.ylabel('Forces [N]')
         plt.grid(True)
         return fig
 
     def plot_friction_forces_ori(
-        self,
-        times: NDARRAY_V1,
-        ori: int,
+            self,
+            times: NDARRAY_V1,
+            ori: int,
     ) -> Figure:
         """Plot friction forces"""
-        fig = plt.figure(f"Friction forces (ori={ori})")
+        fig = plt.figure(f'Friction forces (ori={ori})')
         for sensor_i in range(self.size(1)):
             data = np.asarray(self.friction_all(sensor_i))
             plt.plot(
                 times,
-                data[: len(times), ori],
-                label=f"Leg_{sensor_i}",
+                data[:len(times), ori],
+                label=f'Leg_{sensor_i}',
             )
         plt.legend()
-        plt.xlabel("Times [s]")
-        plt.ylabel("Forces [N]")
+        plt.xlabel('Times [s]')
+        plt.ylabel('Forces [N]')
         plt.grid(True)
         return fig
 
     def plot_total_forces(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot contact forces"""
-        fig = plt.figure("Contact total forces")
+        fig = plt.figure('Contact total forces')
         for sensor_i in range(self.size(1)):
             data = np.asarray(self.total_all(sensor_i))
             plt.plot(
                 times,
-                np.linalg.norm(data, axis=-1)[: len(times)],
-                label=f"Leg_{sensor_i}",
+                np.linalg.norm(data, axis=-1)[:len(times)],
+                label=f'Leg_{sensor_i}',
             )
-        plt.legend(
-            bbox_to_anchor=(1.05, 1),
-            borderaxespad=0,
-        )
-        plt.xlabel("Times [s]")
-        plt.ylabel("Forces [N]")
+        plt.legend()
+        plt.xlabel('Times [s]')
+        plt.ylabel('Forces [N]')
         plt.grid(True)
         return fig
 
@@ -1760,9 +1143,9 @@ class XfrcArray(SensorData, XfrcArrayCy):
 
     @classmethod
     def from_names(
-        cls,
-        names: List[str],
-        n_iterations: int,
+            cls,
+            names: List[str],
+            n_iterations: int,
     ):
         """From names"""
         n_sensors = len(names)
@@ -1775,10 +1158,10 @@ class XfrcArray(SensorData, XfrcArrayCy):
 
     @classmethod
     def from_size(
-        cls,
-        n_links: int,
-        n_iterations: int,
-        names: List[str],
+            cls,
+            n_links: int,
+            n_iterations: int,
+            names: List[str],
     ):
         """From size"""
         xfrc = np.full(
@@ -1790,10 +1173,10 @@ class XfrcArray(SensorData, XfrcArrayCy):
 
     @classmethod
     def from_parameters(
-        cls,
-        n_iterations: int,
-        n_links: int,
-        names: List[str],
+            cls,
+            n_iterations: int,
+            n_links: int,
+            names: List[str],
     ):
         """From parameters"""
         return cls(
@@ -1806,91 +1189,434 @@ class XfrcArray(SensorData, XfrcArrayCy):
         )
 
     def force(
-        self,
-        iteration: int,
-        sensor_i: int,
+            self,
+            iteration: int,
+            sensor_i: int,
     ) -> NDARRAY_3_D:
         """Force"""
-        return self.array[iteration, sensor_i, sc.xfrc_force_x : sc.xfrc_force_z + 1]
+        return self.array[iteration, sensor_i, sc.xfrc_force_x:sc.xfrc_force_z+1]
 
     def forces(self) -> NDARRAY_XX3_D:
         """Forces"""
-        return self.array[:, :, sc.xfrc_force_x : sc.xfrc_force_z + 1]
+        return self.array[:, :, sc.xfrc_force_x:sc.xfrc_force_z+1]
 
     def set_force(
-        self,
-        iteration: int,
-        sensor_i: int,
-        force: NDARRAY_3_D,
+            self,
+            iteration: int,
+            sensor_i: int,
+            force: NDARRAY_3_D,
     ):
         """Set force"""
-        self.array[iteration, sensor_i, sc.xfrc_force_x : sc.xfrc_force_z + 1] = force
+        self.array[iteration, sensor_i, sc.xfrc_force_x:sc.xfrc_force_z+1] = force
 
     def torque(
-        self,
-        iteration: int,
-        sensor_i: int,
+            self,
+            iteration: int,
+            sensor_i: int,
     ) -> NDARRAY_3_D:
         """Torque"""
-        return self.array[iteration, sensor_i, sc.xfrc_torque_x : sc.xfrc_torque_z + 1]
+        return self.array[iteration, sensor_i, sc.xfrc_torque_x:sc.xfrc_torque_z+1]
 
     def torques(self) -> NDARRAY_XX3_D:
         """Torques"""
-        return self.array[:, :, sc.xfrc_torque_x : sc.xfrc_torque_z + 1]
+        return self.array[:, :, sc.xfrc_torque_x:sc.xfrc_torque_z+1]
 
     def set_torque(
-        self,
-        iteration: int,
-        sensor_i: int,
-        torque: NDARRAY_3_D,
+            self,
+            iteration: int,
+            sensor_i: int,
+            torque: NDARRAY_3_D,
     ):
         """Set torque"""
-        self.array[
-            iteration, sensor_i, sc.xfrc_torque_x : sc.xfrc_torque_z + 1
-        ] = torque
+        self.array[iteration, sensor_i, sc.xfrc_torque_x:sc.xfrc_torque_z+1] = torque
 
     def plot(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Dict:
         """Plot"""
         return {
-            "forces": self.plot_forces(times),
-            "torques": self.plot_torques(times),
+            'forces': self.plot_forces(times),
+            'torques': self.plot_torques(times),
         }
 
     def plot_forces(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot"""
-        fig = plt.figure("External forces")
+        fig = plt.figure('External forces')
         for link_i in range(self.size(1)):
-            data = np.asarray(self.forces())[: len(times), link_i]
+            data = np.asarray(self.forces())[:len(times), link_i]
             plt.plot(
                 times,
                 np.linalg.norm(data, axis=-1),
-                label=f"Link_{link_i}",
+                label=f'Link_{link_i}',
             )
-        plt.xlabel("Time [s]")
-        plt.ylabel("Forces [N]")
+        plt.xlabel('Time [s]')
+        plt.ylabel('Forces [N]')
         plt.grid(True)
         return fig
 
     def plot_torques(
-        self,
-        times: NDARRAY_V1,
+            self,
+            times: NDARRAY_V1,
     ) -> Figure:
         """Plot"""
-        fig = plt.figure("External torques")
+        fig = plt.figure('External torques')
         for link_i in range(self.size(1)):
-            data = np.asarray(self.torques())[: len(times), link_i]
+            data = np.asarray(self.torques())[:len(times), link_i]
             plt.plot(
                 times,
                 np.linalg.norm(data, axis=-1),
-                label=f"Link_{link_i}",
+                label=f'Link_{link_i}',
             )
-        plt.xlabel("Time [s]")
-        plt.ylabel("Torques [Nm]")
+        plt.xlabel('Time [s]')
+        plt.ylabel('Torques [Nm]')
         plt.grid(True)
         return fig
+
+
+class MusclesArray(SensorData, MusclesArrayCy):
+    """ Muscles array """
+
+    @classmethod
+    def from_names(
+            cls,
+            names: List[str],
+            n_iterations: int,
+    ):
+        """From names"""
+        n_sensors = len(names)
+        array = np.full(
+            shape=[n_iterations, n_sensors, sc.muscle_size],
+            fill_value=0,
+            dtype=NPDTYPE,
+        )
+        return cls(array, names)
+
+    @classmethod
+    def from_size(
+            cls,
+            n_links: int,
+            n_iterations: int,
+            names: List[str],
+    ):
+        """From size"""
+        muscles = np.full(
+            shape=[n_iterations, n_links, sc.muscle_size],
+            fill_value=0,
+            dtype=NPDTYPE,
+        )
+        return cls(muscles, names)
+
+    @classmethod
+    def from_parameters(
+            cls,
+            n_iterations: int,
+            n_links: int,
+            names: List[str],
+    ):
+        """From parameters"""
+        return cls(
+            np.full(
+                shape=[n_iterations, n_links, sc.muscle_size],
+                fill_value=0,
+                dtype=NPDTYPE,
+            ),
+            names,
+        )
+
+    def excitation(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle excitation of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_excitation]
+
+    def excitations(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle excitations of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_excitation]
+
+    def excitations_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle excitations of all muscles """
+        return self.array[:, :, sc.muscle_excitation]
+
+    def activation(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle activation of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_activation]
+
+    def activations(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle activations of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_activation]
+
+    def activations_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle activations of all muscles """
+        return self.array[:, :, sc.muscle_activation]
+
+    def mtu_length(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle tendon unit length of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_tendon_unit_length]
+
+    def mtu_lengths(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle tendon unit lengths of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_tendon_unit_length]
+
+    def mtu_lengths_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle tendon unit lengths of all muscles """
+        return self.array[:, :, sc.muscle_tendon_unit_length]
+
+    def mtu_velocity(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle tendon unit velocity of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_tendon_unit_velocity]
+
+    def mtu_velocities(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle tendon unit velocities of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_tendon_unit_velocity]
+
+    def mtu_velocities_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle tendon unit velocities of all muscles """
+        return self.array[:, :, sc.muscle_tendon_unit_velocity]
+
+    def mtu_force(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle tendon unit force of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_tendon_unit_force]
+
+    def mtu_forces(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle tendon unit forces of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_tendon_unit_force]
+
+    def mtu_forces_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle tendon unit forces of all muscles """
+        return self.array[:, :, sc.muscle_tendon_unit_force]
+
+    def fiber_length(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle fiber length of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_fiber_length]
+
+    def fiber_lengths(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle fiber lengths of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_fiber_length]
+
+    def fiber_lengths_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle fiber lengths of all muscles """
+        return self.array[:, :, sc.muscle_fiber_length]
+
+    def fiber_velocity(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle fiber velocity of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_fiber_velocity]
+
+    def fiber_velocities(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle fiber velocities of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_fiber_velocity]
+
+    def fiber_velocities_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle fiber velocities of all muscles """
+        return self.array[:, :, sc.muscle_fiber_velocity]
+
+    def active_force(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle active force of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_active_force]
+
+    def active_forces(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle active forces of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_active_force]
+
+    def active_forces_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle active forces of all muscles """
+        return self.array[:, :, sc.muscle_active_force]
+
+    def passive_force(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Muscle passive force of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_passive_force]
+
+    def passive_forces(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Muscle passive forces of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_passive_force]
+
+    def passive_forces_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Muscle passive forces of all muscles """
+        return self.array[:, :, sc.muscle_passive_force]
+
+    def tendon_length(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Tendon unit length of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_tendon_length]
+
+    def tendon_lengths(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Tendon unit lengths of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_tendon_length]
+
+    def tendon_lengths_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Tendon unit lengths of all muscles """
+        return self.array[:, :, sc.muscle_tendon_length]
+
+    def tendon_force(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Tendon unit force of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_tendon_force]
+
+    def tendon_forces(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Tendon unit forces of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_tendon_force]
+
+    def tendon_forces_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Tendon unit forces of all muscles """
+        return self.array[:, :, sc.muscle_tendon_force]
+
+    def Ia_feedback(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Type Ia feedback  of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_Ia_feedback]
+
+    def Ia_feedbacks(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Type Ia feedback of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_Ia_feedback]
+
+    def Ia_feedbacks_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Type Ia feedback of all muscles """
+        return self.array[:, :, sc.muscle_Ia_feedback]
+
+    def II_feedback(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Type II feedback  of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_II_feedback]
+
+    def II_feedbacks(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Type II feedback of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_II_feedback]
+
+    def II_feedbacks_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Type II feedback of all muscles """
+        return self.array[:, :, sc.muscle_II_feedback]
+
+    def Ib_feedback(
+            self,
+            iteration: int,
+            muscle_i: int,
+    ) -> float:
+        """ Type Ib feedback  of a muscle at iteration """
+        return self.array[iteration, muscle_i, sc.muscle_Ib_feedback]
+
+    def Ib_feedbacks(
+            self,
+            iteration: int,
+    ) -> NDARRAY_V1_D:
+        """ Type Ib feedback of all muscles at iteration """
+        return self.array[iteration, :, sc.muscle_Ib_feedback]
+
+    def Ib_feedbacks_all(
+            self,
+    ) -> NDARRAY_V2_D:
+        """ Type Ib feedback of all muscles """
+        return self.array[:, :, sc.muscle_Ib_feedback]
diff --git a/farms/farms_mujoco/farms_mujoco/simulation/simulation.py b/farms/farms_mujoco/farms_mujoco/simulation/simulation.py
index 7bb7e8af..f7d5576e 100644
--- a/farms/farms_mujoco/farms_mujoco/simulation/simulation.py
+++ b/farms/farms_mujoco/farms_mujoco/simulation/simulation.py
@@ -92,10 +92,7 @@ class Simulation:
             **kwargs,
     ):
         """From SDF"""
-        substeps = max(1, simulation_options.num_sub_steps)
         mjcf_model, base_link, hfield = setup_mjcf_xml(
-            timestep=simulation_options.timestep/substeps,
-            discardvisual=simulation_options.headless,
             simulation_options=simulation_options,
             animat_options=animat_options,
             arena_options=arena_options,
@@ -176,7 +173,6 @@ class Simulation:
             iteration: int,
             log_path: str = '',
             plot: bool = False,
-            video: str = '',
             **kwargs,
     ):
         """Postprocessing after simulation"""
@@ -204,19 +200,4 @@ class Simulation:
 
         # Plot
         if plot:
-            self.task.data.plot(times)
-
-        # Record video
-        # # if video and self.interface is not None:
-        #     self.interface.video.save(
-        #         video,
-        #         iteration=iteration,
-        #         writer=kwargs.pop('writer', 'ffmpeg')
-        #     )
-        self.task._callbacks[-1].save(
-            filename = video,
-            iteration = iteration,
-            writer=kwargs.pop('writer', 'ffmpeg'),
-        )
-
-        return
+            self.task.data.plot_sensors(times)
